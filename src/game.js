const paragraphElement = document.createElement('p');
const textNode = document.createTextNode('this paragraph was created in code');
paragraphElement.appendChild(textNode);
document.body.appendChild(paragraphElement);
